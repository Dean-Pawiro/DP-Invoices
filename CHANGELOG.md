# Changelog

## vNext

### Added
 - Default master user (master/master) is created automatically on first run

### Changed
- Removed unused dependencies: pdfmake (backend), @testing-library/user-event, invoice-app (frontend)
- Added missing dependencies: express-session, bcryptjs (backend), eslint-config-react-app (frontend)
- Ran depcheck to verify dependency usage

### Removed


Ready for GitHub push.
